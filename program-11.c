/*Program to find positive number or not*/
#include<stdio.h>
main()
{
	int num;
	printf("\n Enter a number:");
	scanf("%d", &num);
	if (num>0)//using if statement
	printf("\n %d is a positive number", num);
	printf("\n End of the program");
}
